# This file is documentation only. Social Memory does not auto-load it.
SOCIAL_MEMORY_DATA_DIR=/absolute/path/to/social-memory-data

# Optional X OAuth 2.0 user access token. Use a separate variable per account.
# SOCIAL_MEMORY_X_ACCESS_TOKEN=
