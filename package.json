{
  "name": "social-memory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "social-memory": "./src/cli.mjs"
  },
  "engines": {
    "node": ">=22.5"
  },
  "scripts": {
    "test": "node --disable-warning=ExperimentalWarning --test",
    "check": "for file in src/*.mjs src/connectors/*.mjs; do node --check \"$file\" || exit 1; done"
  }
}
