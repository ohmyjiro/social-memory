{
  "name": "social-memory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.5"
  },
  "scripts": {
    "test": "node --disable-warning=ExperimentalWarning --test",
    "check": "node --check src/config.mjs && node --check src/db.mjs"
  }
}
